import { ViewStatusOrderAtentionPipe } from './view-status-order-atention.pipe';

describe('ViewStatusOrderAtentionPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewStatusOrderAtentionPipe();
    expect(pipe).toBeTruthy();
  });
});
