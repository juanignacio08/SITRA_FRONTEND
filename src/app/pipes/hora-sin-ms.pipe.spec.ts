import { HoraSinMsPipe } from './hora-sin-ms.pipe';

describe('HoraSinMsPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraSinMsPipe();
    expect(pipe).toBeTruthy();
  });
});
