import { ViewIconStatusOrdenAtentionPipe } from './view-icon-status-orden-atention.pipe';

describe('ViewIconStatusOrdenAtentionPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewIconStatusOrdenAtentionPipe();
    expect(pipe).toBeTruthy();
  });
});
