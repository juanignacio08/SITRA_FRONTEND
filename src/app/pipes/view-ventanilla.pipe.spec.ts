import { ViewVentanillaPipe } from './view-ventanilla.pipe';

describe('ViewVentanillaPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewVentanillaPipe();
    expect(pipe).toBeTruthy();
  });
});
